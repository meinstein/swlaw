import { colors } from '../../styles'

export default {
  container: {
    textAlign: 'center',
    fontSize: 18,
    padding: '15px 24px',
    backgroundColor: colors.black1,
    color: 'white'
  }
}
